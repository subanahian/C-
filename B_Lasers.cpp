#include <iostream>
using namespace std;

int main() {
    int t;
    cin >> t; 
    while(t--) {
        int n, m;
        long long x, y;
        cin >> n >> m >> x >> y;
        for(int i=0; i<n; i++) {
            long long temp;
            cin >> temp;
        }
        for(int j=0; j<m; j++) {
            long long temp;
            cin >> temp;
        }
        cout << (n + m) << "\n";
    }
    return 0;
}
